# 项目协作说明

## 项目基本信息

- 项目根目录：`D:\api-test\day02`
- 后端主代码：`day06.py`
- 前端页面：`index.html`
- 项目功能：基于智谱 AI 大模型提供简历优化能力。用户输入目标岗位和原始简历内容后，后端调用 AI 生成简历问题分析、优化后的表达和修改理由，前端负责提交表单并展示结果。

## 运行方式

### 1. 安装依赖

在项目根目录 `D:\api-test\day02` 下运行：

```bash
pip install -r requirements.txt
```

### 2. 配置环境变量

项目通过 `.env` 保存智谱 AI API 密钥，变量名为：

```text
ZHIPUAI_API_KEY
```

安全要求：

- 不允许读取 `.env` 的具体内容。
- 不允许打印 `.env` 或其中的密钥。
- 不允许提交 `.env` 到版本控制系统。

### 3. 启动后端

在项目根目录运行：

```bash
uvicorn day06:app --reload
```

后端默认地址通常是：

```text
http://127.0.0.1:8000
```

### 4. 打开前端

直接用浏览器打开项目根目录下的 `index.html`，页面会调用本地后端接口。

## 代码结构

- `day06.py`：后端主文件，包含 FastAPI 应用、接口定义、智谱 AI 客户端初始化、简历优化逻辑和命令行入口。
- `index.html`：前端页面，负责收集目标岗位和简历内容，并调用后端接口展示结果。
- `requirements.txt`：Python 依赖列表。
- `.env`：本地环境变量文件，只用于保存敏感密钥，不允许读取、打印或提交。

## 禁止提交的内容

以下内容不允许提交到版本控制系统：

- `.env`
- `.venv/`
- `__pycache__/`
- `.idea/`

## 修改规则

- 修改代码前必须先给出简短计划，说明准备改哪些文件、为什么改、如何验证。
- 每次只做小步修改，避免一次性改动过多无关内容。
- 除非用户明确要求，不要修改与当前任务无关的文件。
- 修改后必须告诉用户如何测试。
- 修改后应说明实际改了哪些内容。

## 测试建议

当前项目没有自动化测试框架。修改后至少进行以下手动验证：

1. 启动后端：

```bash
uvicorn day06:app --reload
```

2. 检查健康接口：

```bash
curl http://127.0.0.1:8000/health
```

预期返回：

```json
{"status":"ok"}
```

3. 打开 `index.html`，输入目标岗位和简历内容，分别点击“结构化优化简历”和“流式优化简历”，确认页面能正常显示 AI 返回结果。
