# 项目文档

## 项目目标

本项目旨在提供一个基于智谱AI大模型的简历优化工具。用户输入目标岗位和原始简历内容后，AI将分析简历问题，并根据目标岗位优化表达，生成一份更具招聘竞争力的简历。

## 运行方式

### 1. 环境准备

确保你已经安装了 Python 3.9+。

### 2. 安装依赖

在项目根目录下，运行以下命令安装所需的 Python 依赖：

```bash
pip install -r requirements.txt
```

### 3. 配置 API 密钥

创建或编辑项目根目录下的 `.env` 文件，添加你的智谱 AI API 密钥：

```
ZHIPUAI_API_KEY="你的智谱AI API 密钥"
```

**重要提示**：请勿将你的 API 密钥直接提交到版本控制系统。

### 4. 运行项目

配置完成后，运行 `main.py` 文件：

```bash
python main.py
```

程序会提示你输入目标岗位和简历内容。

## 代码结构

- `main.py`: 项目主文件，包含 AI 客户端初始化、简历优化逻辑 (`ask_ai` 函数) 和命令行交互界面。
- `requirements.txt`: 记录项目所需的 Python 依赖库。
- `.env`: 环境变量配置文件，用于存储敏感信息如 API 密钥。

## 不要随意修改的文件

- `.venv/`: Python 虚拟环境目录，由 `pip` 管理，请勿手动修改其内容。
- `requirements.txt`: 除非添加或移除项目依赖，否则不建议随意修改。
- `.env`: 仅用于配置环境变量，请勿在其中编写程序逻辑。
- `__pycache__/`: Python 解释器生成的缓存文件，无需关注。

## 编码风格

本项目遵循 Python 的 PEP 8 编码规范。请注意以下几点：

- 变量和函数名使用 `snake_case` (小写字母和下划线)。
- 类名使用 `CamelCase` (驼峰命名法)。
- 保持代码的简洁性和可读性。
- 适当的注释用于解释复杂逻辑或非显而易见的决策。

## 测试命令

目前本项目没有设置自动化测试框架。建议在修改代码后，通过运行 `main.py` 并提供不同的输入来手动验证功能。

未来可以考虑引入 `pytest` 等测试框架，为 `ask_ai` 函数编写单元测试和集成测试。
